<% if current_user %>
  Welcome <%= current_user.screen_name %> <%= current_user.twitter_name %>!
  <%= link_to "Sign Out", signout_path %>
<% else %>
  <%= link_to "Sign in with Twitter", "/auth/twitter" %>
<% end %>
つぶやく時間は９時だよ<br>
<%= link_to('次へ', bot_path(params[:id])) %>